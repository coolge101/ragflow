# RAGFlow Harness Engineering 策略

## 概述
Harness Engineering（约束/控制工程）旨在为AI系统建立安全边界和控制机制，确保系统在预设范围内安全、可靠地运行。针对RAGFlow项目，我们需要建立一个全面的AI安全约束体系。

本策略将 Harness 明确为两个互补子域，便于分工与文档演进：

| 子域 | 关注点 | 典型载体 |
|------|--------|----------|
| **Runtime / AI Harness** | 运行时安全、对抗测试、监控与审计 | `HarnessMonitor`、对抗测试、安全 CI |
| **Delivery / Contract Harness** | 交付物可审阅、可合并、契约可治理 | Git 工作区卫生、PR 批次、结构化日志/Schema 版本与弃用路径 |

二者共同目标：**可预测、可审计、可回滚**；后者直接承接人机协作与 AI 辅助开发中的工程风险（见下文「交付与契约 Harness」）。

## 当前状态分析
RAGFlow已有完善的测试基础设施：
- ✅ 单元测试框架 (pytest)
- ✅ 集成测试 (HTTP API, SDK API)
- ✅ 端到端测试 (Playwright)
- ✅ 安全沙箱测试 (sandbox_security_tests_full.py)
- ✅ CI/CD流水线 (GitHub Actions)
- ✅ 代码覆盖率报告

## Harness Engineering 核心组件

### 1. AI安全约束框架
#### 1.1 输入验证与过滤
- **提示注入防御**：检测和阻止恶意提示注入
- **内容安全策略**：过滤不当或有害内容
- **输入长度限制**：防止资源耗尽攻击

#### 1.2 输出安全控制
- **内容审查**：确保输出符合安全策略
- **事实核查**：验证生成内容的准确性
- **毒性检测**：识别和过滤有害输出

#### 1.3 资源限制
- **执行时间限制**：防止无限循环
- **内存使用限制**：防止内存耗尽
- **API调用限制**：防止滥用外部服务

### 2. 可解释性与监控系统
#### 2.1 决策追踪
- **推理链记录**：保存LLM的完整推理过程
- **来源追溯**：追踪生成内容的原始数据来源
- **置信度评分**：提供输出的置信度评估

#### 2.2 实时监控
- **性能指标**：响应时间、吞吐量、错误率
- **安全事件**：异常行为检测和报警
- **质量指标**：生成内容的相关性和准确性

#### 2.3 审计日志
- **完整操作记录**：用户操作、系统响应、资源使用
- **合规性报告**：满足监管要求的审计跟踪
- **异常检测**：自动识别可疑模式

### 3. 故障安全机制
#### 3.1 优雅降级
- **备用策略**：当主要组件失败时的备用方案
- **限流机制**：防止系统过载
- **熔断器模式**：快速失败避免级联故障

#### 3.2 恢复策略
- **自动恢复**：从故障状态自动恢复
- **数据一致性**：确保故障后的数据完整性
- **状态管理**：维护可恢复的会话状态

### 4. 对抗性防御
#### 4.1 红队测试
- **对抗性提示**：测试系统对恶意输入的抵抗能力
- **边界测试**：探索系统的极限情况
- **模糊测试**：随机输入测试系统的稳定性

#### 4.2 安全强化
- **沙箱隔离**：代码执行的完全隔离
- **权限最小化**：限制系统组件的访问权限
- **加密通信**：保护数据传输的安全性

### 5. 交付与契约 Harness（Delivery / Contract Harness）

本节将「工程过程」纳入 Harness：约束 AI 辅助开发与人机协作中的交付行为，降低审阅成本与契约漂移风险。动机来自工程复盘中的三类问题（详见文末 **文档修订记录**）。

#### 5.1 工作区与暂存范围（交付卫生）

- **原则**：合并前工作区应可解释；PR 内文件变更应与 PR 描述一致。
- **约束**：
  - 禁止在未核对前使用 `git add -A` 将无关未跟踪文件（如本地 `logs/`、误生成的 lock、个人笔记）带入提交。
  - 推荐显式路径暂存（`git add <path>…`）或 `git add -p`；大型无关改动应先 `git stash` 或另开分支。
- **与 Runtime Harness 的关系**：减少「不可审计的噪声变更」，使安全与功能审阅聚焦真实意图。

#### 5.2 变更批次与 PR 粒度

- **原则**：默认以「一个可审阅主题」为合并单元，而非机械的一行/一字段一 PR。
- **约束**：
  - 同一主题下的文档、契约测试、实现宜同批次审阅，避免 CI 与 reviewer 被碎片化 PR 淹没。
  - 例外：紧急热修、必须隔离回滚风险的单点修改、或仓库策略明确要求的最小变更。
- **与 CI 的关系**：若工作流（如 `.github/workflows/harness_engineering.yml`）在每次 push 上成本较高，优先 consolidation branch 或少量 PR 承载同一主题。

#### 5.3 可观测输出与契约字段治理

- **原则**：结构化日志、JSON Schema、示例文件等「机器可读契约」的变更须可版本化、可测试、可迁移。
- **约束**：
  - 新增字段前评估：**下游是否消费**、是否与现有字段**语义重复**；避免仅为「自解释」无限堆叠同义布尔或镜像字段。
  - 破坏性或大表面积变更：采用 **版本号**（如 `log_version`）、**弃用列表**、**兼容开关**（如 `TBOX_WEBHOOK_LOG_COMPAT_V1`）与 **契约测试** 同 PR 落地；文档与实现不得漂移。
- **参考实践**：子项目中对 webhook 校验脚本 `start` 日志的 Phase A/B 压缩与提案文档，可作为本策略的操作范例（路径随仓库模块而定）。

## 实施路线图

### 阶段1：基础安全约束（1-2周）
1. 扩展现有沙箱安全测试
2. 实现输入验证中间件
3. 添加资源限制机制
4. 建立基本监控指标

### 阶段2：可解释性增强（2-3周）
1. 实现推理链记录
2. 添加来源追溯功能
3. 建立审计日志系统
4. 开发监控仪表板

### 阶段3：高级安全特性（3-4周）
1. 实现提示注入检测
2. 添加内容安全过滤
3. 建立优雅降级机制
4. 实施红队测试框架

### 阶段4：集成与优化（2-3周）
1. 集成到CI/CD流水线
2. 性能优化和调优
3. 文档和培训
4. 持续改进流程

### 交付与契约 Harness（持续）

1. 在贡献指南与 PR 模板中引用本节 5.1–5.3（与 `AGENTS.md` / `CLAUDE.md` 对齐，避免重复叙述）。
2. 对关键契约（Schema、校验脚本输出）维护提案文档 + 契约测试 + 修订记录。
3. 每季度复盘：PR 粒度、误提交率、契约字段增长率。

## 技术栈选择

### 安全框架
- **Python安全库**：`bandit`、`safety`、`semgrep`
- **输入验证**：`pydantic`、`cerberus`
- **内容过滤**：`profanity-filter`、`detoxify`

### 监控系统
- **指标收集**：Prometheus + Grafana
- **日志管理**：ELK Stack (Elasticsearch, Logstash, Kibana)
- **分布式追踪**：Jaeger或OpenTelemetry

### 测试框架
- **安全测试**：扩展现有`pytest`框架
- **负载测试**：`locust`或`k6`
- **混沌工程**：`chaostoolkit`

## 集成到现有架构

### 1. 中间件层
在API层添加安全中间件：
```python
class SecurityMiddleware:
    def validate_input(self, request):
        # 输入验证逻辑
        pass

    def sanitize_output(self, response):
        # 输出清理逻辑
        pass

    def enforce_limits(self, context):
        # 资源限制逻辑
        pass
```

### 2. 监控代理
在关键组件中添加监控代理：
```python
class MonitoringAgent:
    def track_decision(self, decision_data):
        # 记录决策过程
        pass

    def log_security_event(self, event):
        # 记录安全事件
        pass

    def report_metrics(self):
        # 报告性能指标
        pass
```

### 3. 安全沙箱增强
扩展现有沙箱的安全功能：
```python
class EnhancedSandbox:
    def __init__(self):
        self.resource_limiter = ResourceLimiter()
        self.security_scanner = SecurityScanner()
        self.behavior_monitor = BehaviorMonitor()

    def execute_safely(self, code):
        # 安全执行代码
        pass
```

## 测试策略

### 1. 单元测试
- 测试单个安全组件
- 验证约束逻辑的正确性
- 模拟边界条件

### 2. 集成测试
- 测试组件间的交互
- 验证端到端的安全流程
- 测试故障恢复机制

### 3. 安全测试
- 渗透测试
- 模糊测试
- 红队演练

### 4. 性能测试
- 负载测试
- 压力测试
- 耐久性测试

## 成功指标

### 技术指标
- 安全事件数量减少90%
- 系统可用性达到99.9%
- 平均响应时间<2秒
- 错误率<0.1%

### 业务指标
- 用户信任度提升
- 合规性审计通过率100%
- 安全事故响应时间<15分钟
- 安全培训完成率100%

### 交付与契约（工程）指标（可选纳入季度复盘）

- **PR 可审阅性**：单 PR 主题单一、文件列表可解释；无关路径误入次数趋近于零。
- **契约稳定性**：契约类变更带版本/弃用/测试的比例为 100%。
- **结构化输出复杂度**：单位时间内新增日志/配置字段数与「已弃用/已合并」字段数之差的趋势（避免只增不减）。

## 风险与缓解

### 技术风险
1. **性能影响**：安全检查可能增加延迟
   - 缓解：优化算法，使用缓存，异步处理

2. **误报率**：安全规则可能产生误报
   - 缓解：精细调优规则，人工审核流程

3. **复杂性增加**：系统变得更加复杂
   - 缓解：模块化设计，清晰文档，自动化测试

### 组织风险
1. **团队技能**：需要安全专业知识
   - 缓解：培训现有团队，招聘专家，外部咨询

2. **文化阻力**：开发团队可能抵制额外约束
   - 缓解：强调价值，提供工具支持，逐步引入

## 下一步行动

### 立即行动（第1周）
1. 成立Harness Engineering工作组
2. 审查现有安全测试
3. 制定详细实施计划
4. 准备开发环境

### 短期目标（1-2个月）
1. 实现基础安全约束
2. 建立监控框架
3. 完成第一阶段测试
4. 培训开发团队

### 长期目标（3-6个月）
1. 全面部署Harness Engineering系统
2. 集成到生产环境
3. 建立持续改进流程
4. 获得安全认证

## 结论
Harness Engineering是确保RAGFlow安全可靠运行的关键。通过分阶段实施，我们可以逐步建立全面的AI安全约束体系，同时最小化对现有系统的影响。这个策略将帮助RAGFlow在保持创新性的同时，确保最高级别的安全性和可靠性。

---

## 文档修订记录（有迹可查）

以下记录**修改意见与决策过程**，便于事后审计与再讨论；正文条款以上节号为准。

| 日期 | 触发来源 | 问题归纳 | 文档动作 |
|------|----------|----------|----------|
| 2026-04-30 | 工程复盘（人机协作 / AI 辅助交付） | （1）仓库存在无关未跟踪或脏变更，导致提交时需过度谨慎控制 `git add` 范围，易误带文件。（2）变更粒度过细使 PR 数量膨胀、审阅噪音上升。（3）结构化日志等字段堆叠带来语义冗余与契约治理成本。 | 在概述中拆分 **Runtime / AI Harness** 与 **Delivery / Contract Harness**；新增 **§5 交付与契约 Harness**，写入工作区卫生、PR 批次、契约字段治理三项原则及与 CI 的关系；在实施路线图中增加「交付与契约 Harness（持续）」条目；在成功指标中增加可选「交付与契约」工程指标；本表保留决策痕迹。 |

**设计取舍说明**：

- 不将上述三条仅并入「安全测试」章节，以免读者误以为 Harness 只管红队与注入；交付约束单独成节，与安全并行。
- 具体操作步骤（命令级 checklist）放在 `docs/harness_engineering_guide.md`，策略文件保留原则与指标，避免双处大段重复；与 `AGENTS.md` 的关系为：**Harness 写增量约束，通用 Git 约定以 AGENTS 为准**。
