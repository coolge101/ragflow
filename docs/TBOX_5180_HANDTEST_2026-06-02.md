# TBOX 5180 手测记录 — 2026-06-02

**环境**：`http://127.0.0.1:5180` · 分支 `tbox-deploy` · 账号 `admin@ragflow.io` / `admin`
**范围**：Walkthrough 准生产手测（文档/爬取/对话/检索）+ 当晚 console rebuild

---

## 1. 当晚已交付功能（代码 + 5180 已 rebuild）

| 项 | 说明 | 矩阵 / 路径 |
|----|------|-------------|
| **G1-DOC-ORIGINAL-VIEW** | `/documents` 每行 **「阅读原文」「下载原文」**；`GET /api/v1/documents/:id/preview` | `web-tbox/src/utils/documentOriginal.ts`、`DocumentsPage.tsx` |
| **文档列表分页修复** | 后端 `total_datasets` → 前端 `datasetsListTotal()` | `web-tbox/src/api/datasets.ts` |
| **爬取页 KB 下拉** | `listDatasets(page_size=100)` 避免 API 上限 101 | `CrawlPage.tsx` |
| **矩阵 §8 UX backlog** | G3 答案/Citation/检索呈现待 P2 改进 | `2026-05-24-tbox-capability-matrix-design.md` §8 |
| **Phase 67 规划** | G2 全网发现 + 智能去重（P1 优先） | `2026-06-02-tbox-g2-discover-dedup-plan.md` |

---

## 2. 手测结论摘要

### 2.1 已通过 / 可用

- 登录（5180）、`/documents` 知识库翻页、展开文档列表。
- 爬取任务创建、绑定知识库、执行一次后 **page.html** 入库 **TBOX-法规**（任务「法规爬取」）。
- **阅读原文**：preview API 返回 HTML，新标签页可打开爬取原文。
- Phase 16–17：Citation 点击/侧栏联动、检索结果点击高亮（**功能路径**通过）。

### 2.2 问题与根因（产品/配置，非单点 bug）

| 现象 | 根因 | 处理方向 |
|------|------|----------|
| 爬取内容与「TBOX 法规」期望差大 | 种子为 `https://www.cttic.cn/` **门户首页**；`max_depth` 未扩展；无搜索发现 | **Phase 67**：DISCOVER + DEDUP + 多库任务模板 |
| 无法「全网自动搜、去重入库」 | 当前 `/crawl` 仅种子 URL + 可选 BFS；无搜索 API、无内容 fingerprint | 同上 + **G2-CRAWL-I18N** |
| 对话答非所问（如 TBOX 功能 → 车联网） | KB 语料不足 + query 跑偏 | 补库 + Chat App System Prompt |
| `Provider not found for model .` | 知识库 **embd_id 为空** 或未配 Embedding | `/kb` 配置嵌入模型 |
| Citation / 检索 / 答案 **观感差** | UX backlog | 矩阵 §8 G3-*-UX（P2） |

### 2.3 爬取样本数据（便于复现）

| 字段 | 值 |
|------|-----|
| 任务 | 法规爬取 `72339e405e8911f1906a791f22bf97c8` |
| 目标库 | **TBOX-法规** `03e04d3e4c7e11f197f599d668cf34cb` |
| 文档 | **page.html** `f21aed585e8d11f1906a791f22bf97c8` |
| 种子 URL | `https://www.cttic.cn/` |

---

## 3. 下一阶段开发需求（已写入矩阵 Phase 67）

**四类专题知识库**的爬取均须具备（不限于法规）：

| 知识库域 | 典型内容 | 爬取语言 |
|----------|----------|----------|
| **法规与标准** | TBOX/车联网/信通院等标准、法规、规范性文件 | 中文 + **英文标准/法规同源** |
| **技术发展趋势** | 架构、标准演进、技术路线、白皮书 | 中文 + **英文** |
| **市场与产业趋势** | 市场规模、政策解读、产业链、竞品动态 | 中文 + **英文** |
| **产品与行业情报** | TBOX 产品定义、竞品、行业报告、会议材料 | 中文 + **英文** |

**统一能力（P1 优先）**：

- **G2-CRAWL-DISCOVER** — 搜索词驱动公网 URL 发现（中英 query）
- **G2-CRAWL-DEDUP** — URL 规范化 + 内容 hash，跨 tick/任务/库内去重
- **G2-CRAWL-I18N** — 英文页面抓取、编码与 preview；英/中搜索 provider 配置
- **G2-CRAWL-RELEVANCE**（P2）— 入库前相关性过滤

详见 [`superpowers/plans/2026-06-02-tbox-g2-discover-dedup-plan.md`](./superpowers/plans/2026-06-02-tbox-g2-discover-dedup-plan.md)。

---

## 4. 修订

| 日期 | 说明 |
|------|------|
| 2026-06-02 | 初版：5180 当晚手测 + 交付项 + Phase 67 四类库需求 |
| 2026-06-03 | **Phase 67.0 复测**：commit `6b3c29226`/`e5d459971`；5180 console rebuild 含「搜索发现」UI；API smoke `scripts/tbox_phase67_crawl_smoke.py` — discover 无 Key → `DISCOVER_NO_KEY`；种子 tick → `[tbox:TICK_OK] … skipped_dup_content=1`；**Tavily 实网 discover 待配 `TBOX_CRAWL_TAVILY_API_KEY` 后复测** |

---

## 5. Phase 67.0 复测（2026-06-03）

| 项 | 结果 |
|----|------|
| 5180 console bundle | ✅ 含 `搜索发现` / `tbox_crawl_search_provider` / `套用模板` |
| `tbox_crawl_seen` 表 | ✅ MySQL 建表（`url_canonical_hash` 唯一索引，修复 1071 索引过长） |
| crawl worker | ✅ `ENABLE_TBOX_CRAWL_WORKER=1` 进程在跑 |
| Discover 无 Key | ✅ `POST …/run` → `[tbox:DISCOVER_NO_KEY]` |
| Tavily Key 已配 | ✅ `docker/.env` 已设（未入库） |
| 种子 dedup tick | ✅ `[tbox:TICK_OK] discovered=0 ingested=0 skipped_dup_content=1`（重复执行） |
| Tavily 实网 discover | ☐ **网络阻塞**：VM 无法 HTTPS 访问 Tavily（见 §6）；逻辑层 Key/代码已就绪 |

```bash
# 容器内 API smoke（法规任务默认 ID）
docker exec -e PYTHONPATH=/ragflow docker-ragflow-cpu-1 python3 /ragflow/scripts/tbox_phase67_crawl_smoke.py
```

---

## 6. Phase 67.0 镜像 rebuild + discover smoke（2026-06-04）

| 项 | 结果 |
|----|------|
| `docker compose build ragflow-cpu` | ✅ 镜像 `ragflow-tbox:local` 已重建（Phase 67 代码已 bake，无需再 `docker cp`） |
| `force-recreate ragflow-cpu` | ✅ Key + `ENABLE_TBOX_CRAWL_WORKER=1` 已注入 |
| smoke 脚本 | ✅ 放宽 discover：**不设** `tbox_crawl_allowed_domains`；增加 discover 二次 tick 检查 dedup |
| Tavily 出站 HTTPS | ❌ 当前 VM **无法访问** `https://tavily.com` / `api.tavily.com`（Connection reset）；实网 discover 阻塞在**网络环境**，非应用逻辑 |
| 种子 dedup（rebuild 后） | ✅ 连续两次 `[tbox:TICK_OK] … skipped_dup_content=1` |

**解除 discover 阻塞**：在可访问 Tavily 的网络下重跑：

```bash
bash scripts/tbox_phase67_crawl_smoke.py   # 或容器内同路径
# 期望 discover #1：discovered≥1 且 ingested≥1；discover #2：skipped_dup_* 上升
```

| 日期 | 说明 |
|------|------|
| 2026-06-04 | §6：rebuild `ragflow-cpu` + 放宽 discover smoke + 记录 Tavily 出站网络阻塞 |
