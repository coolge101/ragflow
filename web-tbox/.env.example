# RAGFlow API 基址（宿主访问；开发时由 Vite 代理 /v1 与 /api）
VITE_RAGFLOW_API_ORIGIN=http://127.0.0.1:9380
# 登录接口：本仓库构建的 ragflow-tbox:local（纯 Quart）必须用默认 `/api/v1/auth/login` —— 不要设置下面这行。
# 仅当你跑的是很旧的官方 stock 镜像（例如 infiniflow/ragflow:v0.24.x、Go 混合路由）才取消注释：
# VITE_AUTH_LOGIN_PATH=/v1/user/login
# 无 TBOX 后端时：若 `/v1/tbox/me` 返回非 0 且非 401，仍按登录会话授予默认权限（仅开发兜底，生产勿开）
# VITE_TBOX_OFFLINE_PERMISSIONS=1
# 登录 JSON 未带 is_superuser 时，该邮箱仍按超级用户在控制台放行（默认 admin@ragflow.io；生产可改为空字符串关闭）
# VITE_SUPERUSER_EMAIL=admin@ragflow.io
# 页面确认路由 /review：开发模式默认开启；生产需显式 VITE_REVIEW_PAGES=1；开发中可 VITE_REVIEW_PAGES=0 关闭
# VITE_REVIEW_PAGES=1
