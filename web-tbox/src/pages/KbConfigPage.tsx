export function KbConfigPage() {
  return (
    <div>
      <h1 style={{ marginTop: 0 }}>知识库配置</h1>
      <p className="muted">
        单库嵌入模型、分块与解析选项将对接 RAGFlow 配置 API；危险操作（<code>kb.dangerous</code>）需二次确认。当前为占位页。
      </p>
    </div>
  );
}
