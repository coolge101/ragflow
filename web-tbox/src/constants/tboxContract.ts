/**
 * Expected TBOX HTTP JSON contract (`/v1/tbox/me` shape, crawl fields, etc.).
 * Keep in sync with `api/apps/tbox_app.py` → `TBOX_API_CONTRACT_VERSION`.
 */
export const TBOX_API_CONTRACT_VERSION_EXPECTED = 5 as const;
